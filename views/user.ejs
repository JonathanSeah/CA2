<!-- Jonathan -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Detail | Admin</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body, html {
      height: 100%;
      background-image: linear-gradient(orange, pink);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mt-5">User Detail</h1>
    <a href="/admin" class="btn btn-secondary mb-3">&larr; Back to Admin</a>
    <div class="card shadow-sm">
      <div class="card-body">
        <h3><%= selectedUser.username %> (<%= selectedUser.role %>)</h3>
        <ul class="list-group list-group-flush">
          <li class="list-group-item"><strong>Email:</strong> <%= selectedUser.email_address %></li>
          <li class="list-group-item"><strong>Phone:</strong> <%= selectedUser.phone_number %></li>
          <li class="list-group-item"><strong>Age:</strong> <%= selectedUser.age %></li>
          <li class="list-group-item"><strong>Gender:</strong> <%= selectedUser.gender %></li>
        </ul>
        <div class="mt-3">
          <a href="/updateUser/<%= selectedUser.userID %>" class="btn btn-primary mr-2">Edit</a>
          <form action="/deleteUser/<%= selectedUser.userID %>" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
