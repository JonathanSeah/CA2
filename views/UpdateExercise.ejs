<!-- Jonathan -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Update Exercise</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  </head>
  <body>
    <div class="container py-5">
      <h2 class="mb-4">Update Exercise</h2>

      <% if (message && message.length) { %>
        <div class="alert alert-danger"><%= message %></div>
      <% } %>

      <form action="/updateExercise/<%= exercise.exerciseID %>" method="POST" class="row g-3">
        <div class="col-md-6">
          <label class="form-label">Exercise Name</label>
          <input type="text" name="exercise_name" class="form-control" maxlength="45" required value="<%= exercise.exercise_name %>" />
        </div>

        <div class="col-md-6">
          <label class="form-label">Type</label>
          <input type="text" name="types" class="form-control" maxlength="45" required value="<%= exercise.types %>" />
        </div>

        <div class="col-md-3">
          <label class="form-label">Reps</label>
          <input type="number" name="reps" class="form-control" required value="<%= exercise.reps %>" />
        </div>

        <div class="col-md-3">
          <label class="form-label">Sets</label>
          <input type="number" name="sets" class="form-control" required value="<%= exercise.sets %>" />
        </div>

        <div class="col-12">
          <button class="btn btn-success">Save Changes</button>
          <a href="/dashboard" class="btn btn-secondary ms-2">Cancel</a>
        </div>
      </form>
    </div>
  </body>
</html>
