<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
    body {
        text-align: center; 
    }  
    h1 {
        font-size: 40px; /* Larger font size for the heading */
        font-family: serif, sans-serif; /* Use serif and sans-serif fonts */
        text-align: center; /* Center the heading */
        color: white; /* Dark blue text for the heading */
    }
    body {
        margin: 0%;
        height: 100vh;
        background-image: linear-gradient(red,rgb(172, 172, 5)); /* Light gray background */
    }
    img {
        display: block;
        margin: 0 auto; /* Center the image */
        max-width: 100%; /* Make the image responsive */
    }
</style>
</head>
<body>
    <div class="container">
        <h1 class="mt-5">Welcome to our website</h1>
        <img src="/uploads/C237.gif" alt="Fun animation" class="img-fluid">
        <% if (messages && messages.length > 0) { %>
            <div class="alert alert-success">
                <% messages.forEach(function(message) { %>
                    <p><%= message %></p>
                <% }); %>
            </div>
            Hello , <%= user.username %>
            <br> <a href="/logout" class="btn btn-primary mt-3">Logout</a>
        <% } else { %>
        <a href="/register" class="btn btn-primary mt-3 btn-lg">Register</a>
        <a href="/login" class="btn btn-primary mt-3 btn-lg">Login</a>
        <% } %>
    </div>
</body>
</html>