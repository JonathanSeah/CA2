<!-- Bao Rui-->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Food Log</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container py-5">
      <h2 class="mb-4">Food Log</h2>
      <form method="GET" class="mb-3">
        <input type="text" name="search" placeholder="Search..." value="<%= searchTerm %>" class="form-control w-25 d-inline-block me-2">
        <button type="submit" class="btn btn-secondary">Search Food...</button>
      </form>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Food Name</th>
            <th>Calories</th>
            <th>Carbs (g)</th>
            <th>Protein (g)</th>
            <th>Fats (g)</th>
            <th>Image</th>
            <th>Actions</th> <!-- New column for actions -->
          </tr>
        </thead>
        <tbody>
          <% for (var i = 0; i < foods.length; i++) { %>
            <tr>
              <td><%= foods[i].food_name %></td>
              <td><%= foods[i].calories %></td>
              <td><%= foods[i].carbs %></td>
              <td><%= foods[i].protein %></td>
              <td><%= foods[i].fats %></td>
              <td>
                <img src="/uploads/freddy (1).jpg" alt="Fun animation" class="img-fluid">
              </td>
              <td>
                <!-- Delete button form for each food item -->
                <form action="/deleteFood/<%= foods[i].foodID %>" method="GET">
                  <button type="submit" class="btn btn-danger">Delete</button>
                </form>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
      <div class="d-flex gap-2 mb-3">
        <a href="/dashboard" class="btn btn-secondary">← Back to Dashboard</a>
        <a href="/addFood" class="btn btn-success">+ Add Food</a>
      </div>
    </div>
  </body>
</html>
